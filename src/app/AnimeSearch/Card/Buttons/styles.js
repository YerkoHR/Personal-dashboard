import styled from "styled-components";

export const ContainerBtn = styled.div`
  position: absolute;
  bottom: 10px;
  right: 10px;
  svg {
    transition: 0.5s ease-in-out;
    stroke: #fff;
  }
`;
